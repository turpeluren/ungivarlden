---
export interface SectionProps {
  title: string;
  content: string;
  icon: string;
  alt: string;
  reverse: boolean;
}

const { title, content, icon, alt, reverse } = Astro.props;
---

<div class={`flex ${reverse ? 'flex-row-reverse' : 'flex-row'} max-w-7xl mx-auto bg-page items-center p-6 border border-gray-300 shadow-md pb-6`}>
  <!-- Icon -->
  <img src={icon.src} alt={alt} class="w-12 h-12 mr-4" />

  <!-- Content -->
  <div>
    <h3 class="text-xl font-semibold mb-2">{title}</h3>
    <p>{content}</p>
  </div>
</div>